{
  "development": {
      "username": "root",
      "password": "Abhishek@1",
      "database": "company_bot",
      "host": "127.0.0.1",
      "dialect": "mysql"
  },
  "production": {
  "username": `${DB_USERNAME}`,
  "password": `${DB_PASSWORD}`,
  "database": `${DB_NAME}`,
  "host": `${DB_HOST}`,
  "dialect": "mysql",
  "port": `${DB_PORT}`
}
}
