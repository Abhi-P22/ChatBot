{
  "development": {
      "username": "root",
      "password": "Abhishek@1",
      "database": "company_bot",
      "host": "127.0.0.1",
      "dialect": "mysql"
  },
  "production": {
  "username": `${DB_USER}`,
  "password": `${DB_PASS}`,
  "database": `${DB_NAME}`,
  "host": `${DB_HOST}`,
  "dialect": "mysql",
  "port": `${DB_PORT}`
}
}
